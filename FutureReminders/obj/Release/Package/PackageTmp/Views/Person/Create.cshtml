@model FutureReminders.Models.Person

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Person</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.TextToName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TextToName)
            @Html.ValidationMessageFor(model => model.TextToName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.TextToNumber)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TextToNumber)
            @Html.ValidationMessageFor(model => model.TextToNumber)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
